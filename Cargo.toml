[package]
name = "bevy_symbios_avatar"
version = "0.10.0"
edition = "2024"
authors = ["Janus"]
description = "Bevy integration for parametric avatars on atproto."
license = "MIT"
readme = "README.md"
repository = "https://github.com/TheJanusStream/bevy_symbios_avatar"
keywords = ["avatar", "procedural", "generative", "bevy", "atproto"]
categories = ["game-development", "graphics"]

# docs.rs builds every feature, so the editor's public section functions and the
# clip resource are documented whether or not a default build carries them.
[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[features]
default = ["editor"]
# The record editor, and the only reason anything but bevy and the engine is in
# this tree. It is severable on purpose: a consumer that only wants to *draw* a
# body should not inherit a GUI toolkit, and `default-features = false` is how
# they say so.
editor = ["dep:bevy_egui", "dep:serde_json"]
# Carries the engine's baked CC0 clip set in the binary.
#
# **Off by default, for the same reason it is off upstream.** The artifact is
# 200 KiB, and a consumer that only draws bodies should not pay for motion it
# never plays — least of all a wasm one, where a linked build measured 210 KiB
# larger with it and 125 KiB larger gzipped. A consumer that fetches `clips.bin`
# at run time inserts its own `Clips` resource and pays nothing here.
builtin-clips = ["symbios-avatar/builtin-clips"]

[[example]]
name = "viewer"
# The two windows are most of what this example is for now, so it does not build
# without them — and the clip picker is not a picker with nothing to pick.
required-features = ["editor", "builtin-clips"]

[dependencies]
bevy = "0.19"

# Pinned to one minor line rather than tracked loosely. This crate exists to be
# a *second instrument* on the same body: if the two ever read a different
# version of the engine they stop being comparable, which is the one thing it is
# here to do.
#
# 0.2 rather than 0.1 since the chest axes (chestVolume, chestProjection,
# chestLift) the editor panel reaches landed in that release. Until it was
# published this line was held down by a `[patch.crates-io]` block pointing at a
# sibling directory, which built here and nowhere else (#280).
#
# **0.3 rather than 0.2 since `chestSpacing` and `chestFullness` landed there**
# (#36), which is the same reason one line up with the axes renamed — and it is
# breaking for a 0.x crate, so this crate's own version moves with it. It was
# held down by a `[patch.crates-io]` block again while 0.3.0 was unpublished,
# and that block is gone: `cargo tree -p symbios-avatar` reads
# `symbios-avatar v0.3.0` with no path after it, which is the check that
# matters, because a patch that stops matching its requirement is dropped in
# SILENCE rather than reported.
#
# **0.5 since milestone #11 "Natural walking"** (#41): `Walk::head_level`,
# `Stride::tuck`, `Walk::settle(&Steps)` (which #40 adopts), `Steps::placed`,
# `Steps::doubt`, stance-scoped `Steps::straining`, and `Footholds` — breaking
# again, so this crate's version moves with it again. The milestone's
# `[patch.crates-io]` block is gone with the cut; the same silence trap
# applies, and the same check: `cargo tree -p symbios-avatar` must read
# `v0.5.x` with no `(path)` after it, run AFTER the engine publishes.
#
# **0.6 because the engine took `symbios-texture` 0.5** (#44). Nothing here
# reads that crate, and nothing in the engine's public surface moved — but a
# public dependency's minor move is breaking under 0.x, so the engine cut a
# minor and this crate follows it. No `[patch.crates-io]` was used to reach
# it: the engine published first, which is what makes the silence trap above
# irrelevant this time rather than merely checked.
#
# **0.7 because the engine took `symbios-texture` 0.7** (#45) -- the same shape
# as 0.6 one release earlier, and for the third time nothing here reads that
# crate. The engine also dropped its wasm `getrandom` shim in that release,
# which is a removal a consumer can feel: a wasm application that was getting
# `getrandom`'s `wasm_js` feature through the engine now declares it itself, or
# does not need it. This crate declares no backend either way.
#
# **0.8 because the engine's `Driven` grew two public fields** (engine 0.8.0,
# overlands #1323): `straining` and `lift`. A new public field breaks a struct
# literal or an exhaustive pattern downstream, so the engine cut a minor. This
# crate builds no `Driven` - it only reads one through `From<&Driven>` - so the
# bump moved no code here.
#
# **0.8.1 because the hair material binds the engine's strand mask** (#47,
# engine #340). `strand_mask()` first ships in engine 0.8.1, an additive patch
# release, so the floor moves to it; an 0.8.0 engine does not build this crate.
#
# **0.9 for the helmet family** (#48, engine milestone "Hair overhaul 2026-09"):
# seven helmet scalp styles and sculpted facial hair the editor and the viewer
# name, and the far hair tier `spawn` draws beside the near one. Breaking twice
# over for a 0.x crate: `HairRecord` is no longer `Copy`, and a 0.8 engine
# refuses a whole record wearing a style name it does not know.
symbios-avatar = "0.9.0"

# The first dependency beyond bevy and the engine, and it is deliberate rather
# than accidental. Judging a body off-line is a slow loop — change a constant,
# rebuild, render four fixed angles, hand over a picture — and it cannot answer
# what an axis does across its *range*. A live panel can.
#
# Pinned to the pair known to work against Bevy 0.18 rather than to whatever is
# latest, which is how the sibling project that uses it daily runs.
bevy_egui = { version = "0.41", optional = true }
# Emitting the record is the half that makes the panel worth more than a set of
# sliders, and JSON is what a PDS holds. The engine already depends on this at
# the same version, so it is not a new crate in the tree.
serde_json = { version = "1.0", optional = true }

[dev-dependencies]
# The strip capture loop photographs frames through Bevy's own screenshot path
# and then has to read them back and write one stitched sheet; the stitching
# itself is pure (`src/strips.rs`), and this is only the PNG in and out of it.
# Bevy already builds this crate for its own texture loading, so it is not a
# new crate in the tree; default features off keeps the decoder roster to the
# one format screenshots come in.
image = { version = "0.25", default-features = false, features = ["png"] }

# The viewer example only. The library holds no opinion about the scene a body
# stands in and that includes the camera, so an orbit camera has no business in
# its dependency tree — but the example is where a body is actually looked at,
# and it should be looked at with the same hands as the sibling application.
#
# Its own "bevy_egui" feature is deliberately OFF, which is that application's
# finding rather than a guess: the gate it provides is all-or-nothing and
# privately scheduled, so a right-drag died the moment it crossed a GUI window.
# `gate_camera_on_gui` in the example owns the equivalent, with a held-button
# override.
bevy_panorbit_camera = "0.35"

# The engine and the maths under it at full optimisation even in a debug build.
# Building a body is real numeric work — subdivision, skinning, a megapixel atlas
# — and unoptimised it runs about twenty times slower: the difference between a
# test suite that waits seconds on a build and one that waits minutes.
#
# Named crates rather than `"*"`, which would sweep in the whole of Bevy for no
# gain here and cost CI a very long optimised build of it. These four are where
# the time actually goes.
[profile.dev.package.symbios-avatar]
opt-level = 3
[profile.dev.package.symbios-texture]
opt-level = 3
[profile.dev.package.glam]
opt-level = 3
[profile.dev.package.noise]
opt-level = 3

[lints.rust]
missing_docs = "warn"
# Set by docs.rs only; see the attribute at the top of `src/lib.rs`.
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(docsrs)'] }

[lints.clippy]
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
# A Bevy system takes its parameters by value; that is the contract, not an
# oversight, and there is no way to write one that satisfies this lint.
needless_pass_by_value = "allow"
